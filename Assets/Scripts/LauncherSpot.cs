using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LauncherSpot : MonoBehaviour
{
    // used to move the launcher between positions.
    public Vector2 p0Offset = new Vector2(0.0F, 0.0F);
    public Vector2 p1Offset = new Vector2(0.0F, 0.0F);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
